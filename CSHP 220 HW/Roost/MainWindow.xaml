<Window x:Class="Roost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Roost"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.CommandBindings>
        <CommandBinding x:Name="OnNew" Command="New" CanExecute="OnNew_CanExecute" Executed="OnNew_Executed_1" />
    </Window.CommandBindings>

    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Menu item 1" />
            <MenuItem Header="Menu item 2" />
            <Separator />
            <MenuItem Header="Menu item 3" />
        </ContextMenu>
    </Window.ContextMenu>

    <StackPanel>
        <Menu>
            <MenuItem x:Name="uxNew" Header="_New" Loaded="uxNew_Loaded">
                <MenuItem x:Name="uxNewCollection" Header="_Collection" Click="uxNewCollection_Click" />
                <MenuItem Header="_Open"/>
                <Separator/>
                <MenuItem Header="E_xit" Click="MenuItem_Click"/>
            </MenuItem>
            <MenuItem Header="_Edit"/>
            <MenuItem Header="_Help"/>
        </Menu>

        <Button Content="+">
        </Button>
    </StackPanel>
</Window>
